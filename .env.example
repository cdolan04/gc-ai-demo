# Shopify (auto-managed by `shopify app dev` — do not fill manually)
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SCOPES=read_products,write_products,read_orders,write_orders,read_customers,write_customers,read_inventory,write_inventory,read_content,write_content,read_themes,write_themes,read_discounts,write_discounts,read_locations

# Anthropic (required)
ANTHROPIC_API_KEY=                  # console.anthropic.com

# Klaviyo (stretch goal only)
KLAVIYO_API_KEY=                    # Klaviyo → Settings → API keys → Private key
